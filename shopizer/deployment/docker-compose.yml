version: "3.8"

# ─────────────────────────────────────────────────────────────────────────────
# Shopizer – Unified Local Docker Compose
#
# Usage:
#   cp .env.example .env && edit .env
#   docker compose up -d
#
# Image tags are controlled via .env (IMAGE_TAG_BACKEND, etc.)
# so you can roll forward/back without editing this file.
# ─────────────────────────────────────────────────────────────────────────────

networks:
  # Backend / database traffic (not exposed outside compose)
  internal:
    driver: bridge
  # Frontend → backend traffic
  frontend:
    driver: bridge

volumes:
  mysql_data:
  shopizer_files:   # persistent file storage (product images, etc.)

services:

  # ── MySQL 8 ────────────────────────────────────────────────────────────────
  mysql:
    image: mysql:8.0
    container_name: shopizer-mysql
    restart: unless-stopped
    networks:
      - internal
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-SALESMANAGER}
      MYSQL_USER: ${MYSQL_USER:-shopizer}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-shopizer_pass}
    ports:
      # Expose only on loopback – not accessible from outside host
      - "127.0.0.1:3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --default-authentication-plugin=mysql_native_password
      --innodb-buffer-pool-size=256M
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost",
             "-u", "${MYSQL_USER:-shopizer}",
             "-p${MYSQL_PASSWORD:-shopizer_pass}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 40s

  # ── Spring Boot Backend ────────────────────────────────────────────────────
  shopizer-backend:
    image: shopizer-backend:${IMAGE_TAG_BACKEND:-latest}
    container_name: shopizer-backend
    restart: unless-stopped
    networks:
      - internal
      - frontend
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      # Persistent file storage (uploaded images, documents)
      - shopizer_files:/files
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-default}
      SERVER_PORT: 8080
      APP_FILES_PATH: /files

      # ── Database ──────────────────────────────────────────────────────────
      # Shopizer uses shopizer-core.properties by default; override via env.
      # The property names below match Spring Boot's auto-config.
      # NOTE: Must be a single-line string – YAML >- folds newlines to spaces
      # which produces an invalid JDBC URL with embedded spaces.
      SPRING_DATASOURCE_URL: "jdbc:mysql://mysql:3306/${MYSQL_DATABASE:-SALESMANAGER}?useUnicode=true&characterEncoding=utf-8&autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER:-shopizer}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-shopizer_pass}
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: com.mysql.cj.jdbc.Driver

      # ── JPA / Hibernate ───────────────────────────────────────────────────
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_DATABASE_PLATFORM: org.hibernate.dialect.MySQL8Dialect

      # ── JVM ───────────────────────────────────────────────────────────────
      JAVA_OPTS: >-
        -XX:+UseContainerSupport
        -XX:MaxRAMPercentage=75.0
        -XX:+UseG1GC
        -Djava.security.egd=file:/dev/./urandom

    depends_on:
      mysql:
        condition: service_healthy

    healthcheck:
      test: >
        wget -qO- http://localhost:8080/actuator/health
        | grep -q '"status":"UP"' || exit 1
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s

    # Resource limits (Colima default: 2 CPU, 2 GB RAM)
    deploy:
      resources:
        limits:
          memory: 768M
        reservations:
          memory: 384M

  # ── Angular Admin UI ───────────────────────────────────────────────────────
  shopizer-admin:
    image: shopizer-admin:${IMAGE_TAG_ADMIN:-latest}
    container_name: shopizer-admin
    restart: unless-stopped
    networks:
      - frontend
    ports:
      - "127.0.0.1:${ADMIN_PORT:-8081}:80"
    depends_on:
      shopizer-backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO/dev/null", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  # ── React Shop UI ──────────────────────────────────────────────────────────
  shopizer-react:
    image: shopizer-react:${IMAGE_TAG_REACT:-latest}
    container_name: shopizer-react
    restart: unless-stopped
    networks:
      - frontend
    ports:
      - "127.0.0.1:${SHOP_PORT:-8082}:80"
    depends_on:
      shopizer-backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO/dev/null", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
